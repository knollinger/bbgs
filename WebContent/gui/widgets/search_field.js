var SearchField = function(anchor, searchCallback, itemRenderer) {
    
    this.anchor = anchor;
    this.setupUI();
}

/**
 * 
 */
SearchField.prototype.setupUI = function() {
   
    this.anchor.
    
}